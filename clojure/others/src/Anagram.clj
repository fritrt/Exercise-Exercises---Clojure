(ns anagram
  (:require [clojure.string :as str]))

(defn map-word
  [word]
  (str/split word #""))


(defn anagrams-for [word prospect-list] ;; <- arglist goes here
  ;; your code goes here
  (compare word "listen"))
